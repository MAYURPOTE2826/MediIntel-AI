route:
  receiver: 'default-receiver'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'default-receiver'
    # Mocking out alerts since specific channels aren't configured yet.
    # We can add slack/email/pagerduty configs here later.
